[project]
name = "pullwise-worker"
version = "0.6.2"
description = "Pullwise pull-based scan worker"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
pullwise-worker = "pullwise_worker.main:main"

[tool.setuptools.packages.find]
include = ["pullwise_worker", "codereview", "codereview.*"]
