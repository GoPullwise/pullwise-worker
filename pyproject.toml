[project]
name = "pullwise-worker"
version = "0.3.0"
description = "Pullwise pull-based scan worker"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
pullwise-worker = "pullwise_worker.main:main"

[tool.setuptools]
packages = ["pullwise_worker"]
